server:
  port: 8081

spring:
  application:
    name: application-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/ugc_application_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
